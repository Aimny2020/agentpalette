import React from 'react';
import { PageState } from '../../../shared/ui/PageState';

export function HarnessPage() {
  return <PageState state="empty" title="尚未配置 Harness" description="后续阶段将在此定义项目级工程规则。" />;
}
